# Domain docs

This repository uses a single-context layout.

Before architecture or implementation work:

- Read root `CONTEXT.md` when present.
- Read relevant ADRs under `docs/adr/` when present.
- Use the established domain vocabulary.
- Explicitly flag conflicts with existing ADRs.

Missing domain files are created lazily when real terminology or decisions need recording.
