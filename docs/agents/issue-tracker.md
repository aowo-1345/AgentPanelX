# Issue tracker: Local Markdown

Issues live under `.scratch/<feature-slug>/issues/`.

- Files use `<NN>-<slug>.md`, numbered from `01`.
- Each issue records `Status:` near the top.
- Discussion is appended under `## Comments`.
- “Publish an issue” means creating one of these local Markdown files.
